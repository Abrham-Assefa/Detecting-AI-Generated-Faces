# Detecting AI-Generated Faces

A deep learning project that classifies whether a face image is real or AI-generated.

---

## What is this project about?

AI tools like Stable Diffusion and GANs can now generate human faces that look completely real. These fake faces are being used for fraud, misinformation, and fake accounts. This project builds a model that can automatically detect whether a face is real or synthetically generated.

I trained and compared three different deep learning models on a dataset of real and AI-generated faces, then evaluated which one performs best.

---

## Dataset

I used the **Real and Fake Face Detection** dataset from Kaggle (~140k images). For training I used a subset of 2,500 images per class to keep training manageable on a free GPU.

- **REAL** — genuine human face photographs
- **FAKE** — faces generated by GANs and diffusion models

Train/test split: 80% training, 20% testing.

---

## Models I compared

### 1. ViT-Small (Vision Transformer)
Instead of scanning the image like a CNN, ViT splits the image into small patches and uses self-attention to compare every patch against every other. This makes it very good at catching the subtle, globally-distributed artifacts that AI-generated faces tend to have.

**Result: 97.67% accuracy — best performing model.**

### 2. ResNet-18
A classic convolutional neural network with skip connections (residual blocks). Fast to train, lightweight, and a solid baseline. I chose ResNet-18 over the larger ResNet-50 because the bigger model overfits on this dataset size.

### 3. FFT-ResNet (my custom model)
This is the most experimental model. It runs two parallel streams:
- **Spatial stream** — a regular ResNet-18 looking at pixel information
- **Frequency stream** — converts the image to its Fourier frequency spectrum first, then passes that through another ResNet-18

The idea is that GAN-generated images often leave invisible artifacts in the frequency domain (caused by their upsampling layers) that pixel-based models miss. The two streams are merged at the end for the final prediction.

---

## Training setup

All models were trained with:

- **AdamW optimizer** with weight decay to prevent overfitting
- **Label smoothing** (ε = 0.1) so the model doesn't become overconfident
- **Discriminative learning rates** — the pretrained backbone gets a 10× lower learning rate than the new classification head
- **Cosine learning rate schedule** — gradually reduces the learning rate over training
- **Data augmentation** — random flips, slight rotations, color jitter
- **Best checkpoint saving** — automatically saves the model whenever validation accuracy improves

---

## Extra experiments

**U-Net face segmentation** — I also tried masking out the background before classification, so the model focuses only on the face region and ignores irrelevant parts of the image.

**SimCLR self-supervised learning** — I experimented with pre-training the ResNet-18 encoder without any labels (using contrastive learning), then fine-tuning it on the labeled data. This is useful when you have lots of unlabeled face images available. Even this label-free approach reached 60% accuracy, which shows the learned representations are meaningful.

---

## Results summary

| Model | Test Accuracy | AUC |
|-------|--------------|-----|
| ViT-Small | **97.67%** | 0.996 |
| ResNet-18 | ~89% | — |
| FFT-ResNet | — | — |

---

## How to run it

**Requirements**
```
torch, torchvision, timm, scikit-learn, matplotlib, seaborn, grad-cam, kaggle, tqdm, Pillow, numpy, pandas, opencv-python
```

**Install**
```bash
pip install torch torchvision timm scikit-learn matplotlib seaborn grad-cam kaggle tqdm Pillow numpy pandas opencv-python
```

**Kaggle API setup**

Download your `kaggle.json` token from [kaggle.com/settings](https://www.kaggle.com/settings) and place it at `~/.kaggle/kaggle.json`. The notebook downloads the dataset automatically on first run.

**Run**

Open `AI_Face_Detection_Abrham_Assefa.ipynb` in Jupyter or Google Colab. I recommend Colab with a free T4 GPU — training takes about 8–12 minutes per model.

---

## Project files

```
├── AI_Face_Detection_Abrham_Assefa.ipynb   ← all code is here
├── AI_Face_Detection_Proposal_Abrham_Assefa.docx
└── README.md
```

---

## About

**Author:** Abrham Assefa Habtamu  
**University:** University of Verona  
**Courses:** Computer Vision & Deep Learning, Machine Learning & Deep Learning  
**Academic Year:** 2025–26  
**Professors:** V. Murino, C. Beyan, F. Dibitonto, G. Lucato
